<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top ">
  
  <div
    class="container-fluid d-flex align-items-center justify-content-between flex-nowrap gap-3"
  >
    <!-- Left: Logo + Stayzy + Explore (desktop) -->
    <div class="d-flex align-items-center gap-2 flex-shrink-0">
      <a class="navbar-brand d-flex align-items-center gap-1" href="/listings">
        <i class="fa-solid fa-compass icon"></i>
        <span class="fw-bold d-inline d-md-none" style="color: #fe424d"
          >Stayzy</span
        >
      </a>
      <a class="nav-link plain-link d-none d-md-inline" href="/listings"
        >Explore</a
      >
    </div>

    <div class="search" style="display: flex; align-items: center; justify-content: center;">
    <!-- Center: Search form (desktop only) -->
    <form class="d-none d-md-flex search-form mx-2 my-2 my-md-0" role="search">
      <input
        class="form-control search-box me-2"
        type="search"
        placeholder="Search destinations"
      />
      <button class="btn btn-search" type="submit">
        <i class="fa-solid fa-magnifying-glass"></i>Search
      </button>
    </form>
    </div>

    <!-- Right: Hamburger -->
    <button
      class="navbar-toggler ms-auto"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#mainNavbar"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
  </div>

  <!-- Mobile-only collapse -->
  <div
    class="collapse navbar-collapse bg-white pt-2 px-3 px-md-0"
    id="mainNavbar"
  >
    <!-- Search bar (mobile) -->
     <div class="search" style="display: flex; align-items: center; justify-content: center;">
    <form class="d-flex d-md-none search-form my-2" role="search">
      <input
        class="form-control search-box me-2"
        type="search"
        placeholder="Search destinations"
      />
      <button class="btn btn-search" type="submit">
        <i class="fa-solid fa-magnifying-glass"></i>Search
      </button>
    </form>
    </div>

    <!-- Nav links -->
    <div
      class="ms-md-auto d-flex navbar-nav gap-3 align-items-md-center flex-column flex-md-row text-center text-md-start mt-2 mt-md-0"
    >
      <a class="nav-link plain-link d-md-none" href="/listings">Explore</a>
      <a class="nav-link plain-link" href="/listings/new">Airbnb your home</a>

      <% if (!currUser) { %>
      <a class="nav-link btn-link" href="/signup">Sign Up</a>
      <a class="nav-link btn-link" href="/login" style="margin-right: 1rem;">Log In</a>
      <% } %> <% if (currUser) { %>
      <a class="nav-link btn-link" href="/logout" style="margin-right: 1rem;">Log Out</a>
      <% } %>
    </div>
  </div>
</nav>
